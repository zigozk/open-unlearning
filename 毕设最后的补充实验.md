我建议你**只补一个实验：槽位/题型分层失败模式分析**。

也可以在论文里命名为：

> **细粒度 Slot 与 QA 类型下的遗忘失败模式分析**  
> 或  
> **Slot-level and QA-type-wise Analysis of Fine-grained Unlearning**

这是目前**性价比最高**的补充实验：最贴合你的 MYTOFU 数据集特色，基本不用重新训练，图也最好看。

## 为什么选这个

你的数据本来就有 `primary_slots`、`support_fact_ids`、`qa_type`，并且 forget/retain 本来就是按 slot 划分的；其中 QA 类型还包括 `single_fact` 和 `composite`。这意味着你可以直接按 slot 和题型重新聚合现有评测结果，不需要重新设计任务。

它也最能服务你的论文主线。你的核心贡献不是单纯跑很多方法，而是证明 **slot-based MYTOFU 比整作者遗忘更细粒度、更难、更贴近画像化安全场景**；主实验已经说明 CEU、SimNPO、NPO、RMU 等方法存在遗忘—保留权衡，难度实验也已经说明 LFRP 越高，BUS 越下降。 这个补充实验正好进一步回答：**到底哪些 slot 最难忘？composite 问题是否比 single_fact 更容易泄露？**

## 具体怎么做

选 4 个代表方法就够了：

|方法|作用|
|---|---|
|CEU|强遗忘代表|
|NPO|折中代表|
|RMU|表征干预代表|
|SimNPO_sago 或 SimNPO_pcgrad|高 utility 但忘不干净代表|

不要再放 20 个方法，否则图会太乱。

对每个方法，把评测结果按两类维度聚合：

第一类是 **slot 维度**：

```text
synthetic_private_id
synthetic_contact_token
birth_year
birthplace
father_profession
mother_profession
residence_history
residence_link
...
```

第二类是 **QA 类型维度**：

```text
single_fact
composite
```

每组统计这些指标即可：

```text
forget_Q_A_Prob ↓
exact_memorization ↓
extraction_strength ↓
retain_Q_A_Prob ↑
retain_Q_A_ROUGE ↑
```

如果你有 per-sample 的 `MYTOFU_Mem` 或 truth-ratio，也可以一起按组平均；没有也没关系，`forget_Q_A_Prob` 和 `retain_Q_A_Prob` 已经够直观。

## 最好看的图怎么画

我建议最终放 **3 张图 + 1 个小表**。

**图 1：Forget Slot × Method 热力图**

横轴是方法，纵轴是 forget slots，颜色是 `forget_Q_A_Prob`。

这张图非常适合展示“哪些敏感槽位最难忘”。比如你之前 sanity 里已经发现 `synthetic_private_id`、`residence_history`、地点链路类问题容易混淆，这类现象可以在图里得到更系统的呈现。

**图 2：single_fact vs composite 分组柱状图**

横轴是方法，每个方法两根柱：

```text
single_fact forget leakage
composite forget leakage
```

这张图能回答一个很重要的问题：**复合问题是否更容易绕过遗忘？**  
如果 composite 的泄露率明显更高，你的论文就多了一个很有解释力的结论：细粒度遗忘不只是 slot 难，**多事实组合问法也会放大知识残留**。

**图 3：Slot 难度-泄露散点图**

横轴可以用 slot 的平均 LFRP 或该 slot 的局部纠缠度，纵轴用 `forget_Q_A_Prob` 或 `1 - MYTOFU_Mem`，每个点是一个 slot，颜色表示方法。

这张图能和你已有的 LFRP 难度实验形成呼应：你前面已经证明 split 级别 LFRP 上升会让 BUS 下降；这张图进一步细化到 slot 级别，说明局部知识纠缠如何具体影响遗忘失败。

**小表：Top-5 最难遗忘 slot**

表格列：

```text
Rank | Slot | Avg Forget Prob | Best Method | Worst Method | 可能原因
```

这个表特别适合写分析文字，不只是放图。

## 实验标题可以这样写进论文

建议新增一小节：

> **5.X 不同知识槽位与问题类型下的遗忘失败模式分析**

小节逻辑：

1. 说明 MYTOFU 的优势在于每条 QA 都有 `primary_slots` 和 `qa_type`，所以可以从总分深入到槽位级别分析。
    
2. 按 slot 聚合 forget 侧泄露指标，观察哪些敏感信息最难遗忘。
    
3. 按 `single_fact` / `composite` 聚合，比较简单事实题和复合事实题的差异。
    
4. 总结发现：细粒度遗忘失败往往集中在某些高纠缠槽位和复合问法中，因此只报告整体 Mem/Utility 会掩盖重要失败模式。
    

## 结论预期怎么写

你可以预设一个比较稳妥的分析方向：

> 实验结果表明，不同 slot 的遗忘难度并不均匀。相比出生年份、父母职业等相对孤立的事实，居住经历、地点链路、私人标识等与作者画像高度绑定的知识更容易出现残留。同时，composite 类型问题通常比 single_fact 更容易诱发模型利用残余知识进行组合回答，说明细粒度知识遗忘的困难不仅来自单条事实本身，也来自事实之间的结构关联。

## 为什么不选其他补充实验

多 seed 稳健性当然规范，但要重新跑多轮训练，费算力，图也就是 mean±std，不够丰富。恢复率实验很有价值，但需要设计再学习流程，工程风险高。攻击鲁棒性实验图也好看，但要重新构造 prompt、跑推理、判定泄露，容易拖时间。评测资产质量审计最简单，但更像数据检查，不像一个强实验章节。

所以最终排序是：

```text
最推荐：slot / qa_type 分层失败模式分析
第二选择：攻击 prompt 鲁棒性
第三选择：恢复率实验
不建议现在优先做：多 seed
```

你现在就做 **slot / qa_type 分层失败模式分析**。它最能把你的数据集设计、用户画像边界、细粒度遗忘难度、实验可视化这几条线串起来。